def read_print(string):
    grid = 0
    with open(string) as file:
        str = file.readline()
        gridsize = [int(s) for s in str.split() if s.isdigit()]
        if(len(gridsize) == 3):
            grid = [[[0 for _ in range(gridsize[0])] for _ in range(gridsize[1])] for _ in range(gridsize[2])]
        else:
            print("Incorrect grid size")

        # To do: init gates at coordinates

    file.close()
    return grid

grid = read_print("print_2")

for i in range (len(grid)):
    gridlayer = grid[i]
    for j in range (len(gridlayer)):
        print(gridlayer[j])
    print("\n")